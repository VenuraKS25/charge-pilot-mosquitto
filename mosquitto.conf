# --- Persistence ---
persistence true
persistence_location /mosquitto/data/

# --- Logging ---
# Log to stdout so you can see it in the Railway logs dashboard
log_dest stdout

# --- Listener ---
# Listen on port 1883 on ALL interfaces (0.0.0.0)
listener 1883
allow_anonymous true